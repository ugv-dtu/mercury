ekf_filter_node:
  ros__parameters:

    use_sim_time: true

    frequency: 50.0
    two_d_mode: true

    publish_tf: true

    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: odom

    odom0: /diff_drive_controller/odom
    odom0_config: [true, true, false,
                   false, false, true,
                   true, true, false,
                   false, false, true,
                   false, false, false]

    imu0: /imu
    imu0_config: [false, false, false,
                  true, true, true,
                  false, false, false,
                  true, true, true,
                  false, false, false]

# GPS is processed by navsat_transform for global pose; odom frame uses wheel odometry + IMU only to maintain smooth TF continuity for SLAM.
